# 移工契約文件擷取

跨國人力仲介／移工勞動契約的 DOCX 掃描檔,經 OCR 擷取出許可證、仲介機構與雇主三組
欄位,寫入 Google Sheets。本檔定義本專案討論這些概念時該用的詞;程式碼、issue、
commit、測試命名都照這裡的用語,不要漂移到同義詞。

## 文件與當事人

**許可證**:
私立就業服務機構的官方許可證號(4 碼,分公司帶後綴如 `2340-2`)。整條 pipeline 的主鍵,
Sheets B 欄。
_Avoid_: 證號、licence、permit id、mol 號碼(mol 是其中一個 ROI 的名字,不是這個概念)

**仲介機構**:
契約上的台灣私立就業服務機構(台仲)。特許行業,勞動部有完整名冊,故**查表取得、
不做 OCR**。Sheets E~G 欄。
_Avoid_: 仲介公司、agency、經紀商;指印尼端時一律寫「印尼仲介」不可混稱

**雇主**:
契約甲方,個人或公司。**不在任何名冊**,只能從契約影像 OCR。Sheets H~O 欄。
_Avoid_: 客戶、業主、employer(中文行文時)

**名冊**:
勞動部「跨國人力仲介公司許可名冊」官方 JSON(`data/agency_roster.json`)。
許可證與仲介機構之間的唯一權威對照。
_Avoid_: 資料庫、roster(中文行文時)、名單

**契約頁 / 次頁**:
契約頁是載有雇主欄位的那一頁(靠特徵字加權計分選出);次頁是其後一頁,載有台仲區塊,
供反查救援使用。
_Avoid_: 首頁、第一頁(圖片編號與頁序不保證一致)

## 擷取與決策

**ROI 掃描**:
只對圖片的指定區域(mol / permit_upper / permit_lower)跑 OCR,而非整頁。

**多數票**:
同一值在多組前處理設定下被重複讀到。**重複性本身即第二證據**,故多數票的信心門檻
可低於單次讀值。
_Avoid_: 投票、共識、consensus

**雙引擎交叉比對**:
Tesseract 與 Google Vision 各自讀值後比對。核心設計原則:**任何值要自動寫入,必須有
兩個以上互相獨立的證據**;單一證據一律送驗或標人工審查。
_Avoid_: 二次確認、double check

**獨立證據管道**:
與許可證數字無關、可各自產生候選值的取值路徑。目前有三條:同文件兩 ROI 互證、
多數票、雙引擎一致。反查救援是第四條。
_Avoid_: 備援、fallback(這些暗示主從關係,但各管道地位是對等的)

**反查救援**:
許可證全無命中(或單一低信心判讀)時,改從次頁台仲區塊取電話／名稱／地址,反查名冊
回推許可證。優先序:電話精確 → 電話前綴 → 名稱／地址相似度。命中後**仍標
manual_review**,因為它是獨立證據管道而非第二引擎。
_Avoid_: 補救、backup、fallback

**人工審查 (manual_review)**:
證據不足以自動寫入的狀態,與 `keyed-in` 二選一,理由記在 Sheets D 欄 reason。

**疑慮標示**:
雇主**中文地址**標準化沒能完成時,寫在 Sheets L 欄的原因說明。標的是「這格為什麼空」,
不是「這格可能錯」—— 觸發條件是標準欄留空這個**可觀察的狀態**,不是對值的判斷。
與人工審查是不同的東西:人工審查是「不寫入」的狀態、講許可證;疑慮標示是欄位留空時的
附註、講雇主(見 [ADR-0003](docs/adr/0003-employer-address-doubt-flag.md))。
_Avoid_: 待核、低信心、警告、flag

**縣市非精確命中**:
中文地址行的縣市既配不上完整縣市名、也配不上去尾字,只靠模糊比對湊上。此時**不必然
是錯的** —— 印章咬掉一字的「宜藺縣」正是靠它救回「宜蘭縣」;但配錯時會安靜地換成
另一個縣市,而區名(中山區、中正區)與路名(中山路、中正路)全台通用,錯誤縣市底下
照樣配得到,郵遞區號會跟著一起錯。故此時郵遞區號不回填。

**路名不在資料庫**:
地址文字裡有「路／街／道」樣式,但配不上該行政區的官方路名清單。與「地址無路名」
(鄉村型的村鄰號地址,本來就沒有路名)是兩回事:前者可補進 `data/custom_roads.json`,
後者是正常型態。兩者都是疑慮標示的成因,但要分開講。

## 文字正規化

**異體折疊 (fold_variants)**:
把同一個字／數的不同合法寫法收斂成一種,供比對使用:臺→台、鍾→鐘、全形→半形、
段號國字→阿拉伯。由 `address_db` 單獨擁有,`permit_lookup` 反查時呼叫同一個函式,
不另寫一套(見 [ADR-0001](docs/adr/0001-address-db-owns-tw-text-normalisation.md))。
**產出是比對用中間字串,不可輸出**。
_Avoid_: 正規化(太籠統,本專案另有地址標準化)、normalize

**地址標準化 (normalize_address)**:
用官方門牌庫把 OCR 地址還原成標準地址,並附官方中英譯名與郵遞區號。與異體折疊相反,
它的產出是**可輸出**的,且保留官方正體「臺」。保守回填:縣市＋區＋路全命中才填標準
中文地址欄;郵遞區號另需**縣市精確命中**,寧空勿錯。留空一律掛疑慮標示說明成因。
**只有中文標準地址會輸出** —— 官方英譯不進 Sheets,英文一律以 OCR 原文為準,因為契約
英譯沒有單一正確寫法(Panshih／Panshi 都出現得到),官方譯名並不比契約上的更權威。
_Avoid_: 地址正規化(會與異體折疊混淆)

**英文行錨定**:
紅章常蓋毀中文行首的縣市,而英文地址的縣市慣例在行尾而倖存;故以英文行反查縣市／區
為錨,再回頭用中文行配路名門牌。

**去紅章**:
利用紅印章 R 通道高、黑字三通道皆低的特性,取紅通道為底並抹白偏紅像素。只清白底上的
章,不動壓在黑字上的暗紅像素。
_Avoid_: 去章、除印、deink(中文行文時)
