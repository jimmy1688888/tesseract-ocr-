# 移工契約文件擷取

跨國人力仲介／移工勞動契約的 DOCX 掃描檔,經 OCR 擷取出許可證、仲介機構與雇主三組
欄位,寫入 Google Sheets。本檔定義本專案討論這些概念時該用的詞;程式碼、issue、
commit、測試命名都照這裡的用語,不要漂移到同義詞。

## 文件與當事人

**許可證**:
私立就業服務機構的官方許可證號(4 碼,分公司帶後綴如 `2340-2`)。整條 pipeline 的主鍵,
Sheets B 欄。
_Avoid_: 證號、licence、permit id、mol 號碼(mol 是其中一個 ROI 的名字,不是這個概念)

**仲介機構**:
契約上的台灣私立就業服務機構(台仲)。特許行業,勞動部有完整名冊,故**查表取得、
不做 OCR**。Sheets E~G 欄。
_Avoid_: 仲介公司、agency、經紀商;指印尼端時一律寫「印尼仲介」不可混稱

**雇主**:
契約甲方,個人或公司。**不在任何名冊**,只能從契約影像 OCR。Sheets H~O 欄。
_Avoid_: 客戶、業主、employer(中文行文時)

**名冊**:
勞動部「跨國人力仲介公司許可名冊」官方 JSON(`data/agency_roster.json`)。
許可證與仲介機構之間的唯一權威對照。
_Avoid_: 資料庫、roster(中文行文時)、名單

**契約頁 / 次頁**:
契約頁是載有雇主欄位的那一頁(靠特徵字加權計分選出);次頁是其後一頁,載有台仲區塊,
供反查救援使用。
_Avoid_: 首頁、第一頁(圖片編號與頁序不保證一致)

**切結書**:
`Surat Pernyataan Biaya dan Gaji`(費用及薪資切結書),契約後段的獨立文件,長度
**2~3 頁不定**。**許可證印在它的最後一頁**(簽署方欄 `VI. Pihak yang menandatangani`)。
_Avoid_: 聲明書、切結頁

**圖序**:
docx 內嵌圖的檔名序號(`word/media/imageN`)。前段內容順序是固定的、尾端不是:

```
image1  雜訊/空白  │ image2 契約頁 │ image3 次頁 │ image4~ 切結書 │ 尾端空白圖
```

故許可證落在 image5 或 image6(隨切結書頁數),mol ROI 只有契約頁讀得到。
談位置一律用**絕對序號**,不用「倒數第 N 張」——尾端掛著數量不定的空白圖,
倒數會數到空白(見 [ADR-0005](docs/adr/0005-tiered-permit-image-scan.md))。
_Avoid_: 頁碼、第幾頁(docx 的分頁由 Word 排版決定,zip 裡沒有這個資訊)

## 擷取與決策

**ROI 掃描**:
只對圖片的指定區域(mol / permit_upper / permit_lower)跑 OCR,而非整頁。

**分層掃描**:
許可證只先掃 image 2、5、6(第一層);拿不到 **permit 多數票**才把其餘圖掃完
(第二層)。掃圖佔整條 pipeline 99.7% 的時間,這是唯一有意義的施力點。
**猜錯不會錯值,只會慢** —— 拿不到就擴掃,輸入回到與全掃相同。正因如此它會
安靜地失效(版面一變就每份都擴掃、比全掃更慢而結果一格沒變),故 `matches.csv`
留 `scan_tier` 欄供觀測。停止擴掃的門檻刻意很高:mol 命中不算(那只證明是契約頁)、
`PERMIT_PARTIAL` 也不算(湊不出多數的值會被規則B 推去走反查救援)。
只用於 large;small 最多 3 張圖且只掃小 ROI,套下去會把它掃成零張。
_Avoid_: 快速掃描、預掃、prescan

**多數票**:
同一值在多組前處理設定下被重複讀到。**重複性本身即第二證據**,故多數票的信心門檻
可低於單次讀值。
_Avoid_: 投票、共識、consensus

**雙引擎交叉比對**:
Tesseract 與 Google Vision 各自讀值後比對。核心設計原則:**任何值要自動寫入,必須有
兩個以上互相獨立的證據**;單一證據一律送驗或標人工審查。
_Avoid_: 二次確認、double check

**獨立證據管道**:
與許可證數字無關、可各自產生候選值的取值路徑。目前有三條:同文件兩 ROI 互證、
多數票、雙引擎一致。反查救援是第四條。
_Avoid_: 備援、fallback(這些暗示主從關係,但各管道地位是對等的)

**反查救援**:
許可證全無命中(或單一低信心判讀)時,改從次頁台仲區塊取電話／名稱／地址,反查名冊
回推許可證。優先序:電話精確 → 電話前綴 → 名稱／地址相似度。命中後**仍標
manual_review**,因為它是獨立證據管道而非第二引擎。
_Avoid_: 補救、backup、fallback

**人工審查 (manual_review)**:
證據不足以自動寫入的狀態,與 `keyed-in` 二選一,理由記在 Sheets D 欄 reason。

**疑慮標示**:
雇主資料某一格不可盡信時,寫在 Sheets L 欄的說明,每條冠上欄名(`H:…；J:…`)指明
是哪一格。措辭一律**只陳述確定的事實**,不推斷成因、不給行動建議 —— 因為這條訊息
自己可能是錯的,而一條錯的說明會誘導人做出錯誤判斷(見
[ADR-0003](docs/adr/0003-employer-address-doubt-flag.md) 與
[ADR-0004](docs/adr/0004-employer-name-doubt.md))。
與人工審查是不同的東西:人工審查是「不寫入」的狀態、講許可證;疑慮標示是已寫入
(或已留空)的附註、講雇主。
_Avoid_: 待核、低信心、警告、flag

**門牌號中英比對**:
中文地址的「N號」與英文地址的「No. N」比對,不符即掛疑慮標示。門牌號是阿拉伯數字,
沒有拼音變體的問題 —— 這正是英文地址整體不做判定、卻仍能拿門牌號來對的原因。
「N之M號」與「No. N-M」是同一件事的兩種寫法,比對前統一。

**字數音節數檢查**:
中文雇主名的字數與英文譯名的音節段數比對,不符即掛疑慮標示。是**結構性計數**,
不是音譯比對 —— 音譯有威妥瑪／漢語／通用拼音並存的問題,計數沒有。專治「截斷」:
少了姓的「明芳」(SONG, MING-FANG)看起來完全像個正常名字,人核對時會滑過去。
同字數的錯字抓不到,是已知缺口。
_Avoid_: 音譯比對、姓名驗證

**縣市非精確命中**:
中文地址行的縣市既配不上完整縣市名、也配不上去尾字,只靠模糊比對湊上。此時**不必然
是錯的** —— 印章咬掉一字的「宜藺縣」正是靠它救回「宜蘭縣」;但配錯時會安靜地換成
另一個縣市,而區名(中山區、中正區)與路名(中山路、中正路)全台通用,錯誤縣市底下
照樣配得到,郵遞區號會跟著一起錯。故此時郵遞區號不回填。

**路名未在清單中**:
地址文字裡有「路／街／道」樣式,但配不上該行政區的官方路名清單。**這是一個事實,
不是一個原因** —— 成因可能是該路真的未收錄(32540 的新屋區清華路),也可能是 OCR
把它讀壞了(32510 的梅獅路被讀成梅翠路,那條路好端端在庫裡)。兩者從文字上分不出來,
所以疑慮標示只講事實,不叫人去補 `data/custom_roads.json`。
與「地址無路名」(鄉村型的村鄰號地址,本來就沒有路名)是不同的成因,要分開講。
_Avoid_: 路名不在資料庫(那是推斷,不是事實)

## 文字正規化

**異體折疊 (fold_variants)**:
把同一個字／數的不同合法寫法收斂成一種,供比對使用:臺→台、鍾→鐘、全形→半形、
段號國字→阿拉伯。由 `address_db` 單獨擁有,`permit_lookup` 反查時呼叫同一個函式,
不另寫一套(見 [ADR-0001](docs/adr/0001-address-db-owns-tw-text-normalisation.md))。
**產出是比對用中間字串,不可輸出**。
_Avoid_: 正規化(太籠統,本專案另有地址標準化)、normalize

**地址標準化 (normalize_address)**:
用官方門牌庫把 OCR 地址還原成標準地址,並附官方中英譯名與郵遞區號。與異體折疊相反,
它的產出是**可輸出**的,且保留官方正體「臺」。保守回填:縣市＋區＋路全命中才填標準
中文地址欄;郵遞區號另需**縣市精確命中**,寧空勿錯。留空一律掛疑慮標示說明成因。
**只有中文標準地址會輸出** —— 官方英譯不進 Sheets 也不回填,英文欄純粹是 OCR 讀到什麼
就是什麼、讀不到就空,因為契約英譯沒有單一正確寫法(Panshih／Panshi 都出現得到),
官方譯名並不比契約上的更權威。但英文行仍是**英文行錨定**的輸入 —— 輸出與輸入是兩件事。
_Avoid_: 地址正規化(會與異體折疊混淆)

**英文行錨定**:
紅章常蓋毀中文行首的縣市,而英文地址的縣市慣例在行尾而倖存;故以英文行反查縣市／區
為錨,再回頭用中文行配路名門牌。中文路名也配不到時,再以英文路名對官方英譯做**高門檻**
(0.9)備援 —— 契約英譯是仲介自翻,門檻低了會錯配到同名系路。

**去紅章**:
利用紅印章 R 通道高、黑字三通道皆低的特性,取紅通道為底並抹白偏紅像素。只清白底上的
章,不動壓在黑字上的暗紅像素。
_Avoid_: 去章、除印、deink(中文行文時)
