# ===========================================================================
# DOCX OCR to Google Sheets - Python 依賴清單
# ===========================================================================
# 安裝方式: pip install -r requirements.txt
#
# 注意: 本專案還需要兩個非 Python 的外部程式,需手動安裝:
#   1. Tesseract OCR (含 chi_tra、ind 語言包)
#   2. Ghostscript
# 詳見 README.md
# ===========================================================================

# ── 核心 OCR 引擎 ─────────────────────────────────────────────────────────
ocrmypdf>=16.0.0,<17.0.0          # 對 PDF 加上文字層的主引擎

# ── PDF 處理 ──────────────────────────────────────────────────────────────
pdfplumber>=0.11.0,<0.12.0        # 從 PDF 讀取文字層(OCR 後讀回用)

# ── 影像處理 ──────────────────────────────────────────────────────────────
Pillow>=10.0.0,<12.0.0            # 圖片解碼/編碼/格式轉換
numpy>=1.24.0,<3.0.0              # 紅通道萃取、對比拉伸的向量運算

# ── Google Sheets API ─────────────────────────────────────────────────────
gspread>=6.0.0,<7.0.0             # Google Sheets 操作封裝
google-auth>=2.20.0,<3.0.0        # Service Account 認證
