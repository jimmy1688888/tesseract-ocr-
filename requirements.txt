# ===========================================================================
# DOCX OCR to Google Sheets - Python 依賴清單
# ===========================================================================
# 安裝方式: pip install -r requirements.txt
#
# 注意: 本專案還需要一個非 Python 的外部程式,需手動安裝:
#   Tesseract OCR(含 ind、eng 語言包,預設路徑 C:\Program Files\Tesseract-OCR\)
# 詳見 README.md「安裝步驟」
# ===========================================================================

# ── OCR 引擎 ──────────────────────────────────────────────────────────────
pytesseract>=0.3.10,<0.4.0        # Tesseract 的 Python 封裝(主引擎)

# ── 影像處理 ──────────────────────────────────────────────────────────────
Pillow>=10.0.0,<12.0.0            # 圖片解碼/裁切/前處理
numpy>=1.24.0,<3.0.0              # 紅通道萃取、對比拉伸的向量運算

# ── Google 服務 ───────────────────────────────────────────────────────────
google-cloud-vision>=3.4.0,<4.0.0        # Vision API 交叉比對 / 雇主契約 OCR
google-api-python-client>=2.100.0,<3.0.0 # Sheets API 寫入
google-auth>=2.20.0,<3.0.0               # Service Account 認證

# ── Gemini 離線比對（僅 gemini_bench.py 需要，主流程用不到）─────────────────
# 走 Vertex AI（vertexai=True）而非 AI Studio 免費層：免費層的影像會被用於改善
# Google 產品，而這裡送的是含身分證號的移工契約。見 docs/adr/0008。
google-genai>=1.0.0,<2.0.0

# ── 名冊匯出(僅 export_roster_xlsx.py 需要,主流程用不到)────────────────
openpyxl>=3.1.0,<4.0.0            # 名冊 JSON → Excel,供人工查看

# ── 測試(開發用,同事只跑主程式可略過)─────────────────────────────────
pytest>=8.0.0,<9.0.0
